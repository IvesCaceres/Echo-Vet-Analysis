# Copia este archivo a `.env` y rellena los valores reales.
# `.env` está en .gitignore y NO debe commitearse.

# DSN de PostgreSQL para SQLAlchemy + psycopg 3.x
# Formato: postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
PG_DSN=postgresql+psycopg://USER:PASSWORD@HOST:PORT/DBNAME
